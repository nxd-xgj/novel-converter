小说编码批量转换工具 - Windows 版
==================================
构建方法（需要安装 Python 3.8+）:
  1. 双击 build_windows.bat
  2. 等待自动安装依赖和编译
  3. 完成后在 dist/ 目录找到 NovelConverter.exe

或直接运行（不编译）:
  pip install flask
  python backend.py

启动后浏览器自动打开 http://localhost:5000
